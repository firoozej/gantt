import { useContext } from "react";
import { ThemeContext } from "./Labels";

const SubToolbar = () => {
    //const value = useContext(ThemeContext);
    //console.log("rerender subtoolbar");
    return <></>;
};
export default SubToolbar;
